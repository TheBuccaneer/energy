# RTX 3090 GEMM audit handoff

Install from the project root:

```bash
cd ~/projects/energy/new
unzip -o ~/Downloads/gemm_3090_audit_handoff.zip
```

Files are placed in:

```text
3090/results/GEMM/
```
