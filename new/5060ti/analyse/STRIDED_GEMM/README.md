# GPU STRIDED_GEMM individual audit

Runs the validation and session-aware scientific analysis for the newest complete five-session campaign. Quickcheck CSVs are ignored.

```bash
cd <platform>/analyse/STRIDED_GEMM
./run_all.sh
```

Results are written to `<platform>/results/STRIDED_GEMM/`.
